{
  "name": "kpi-tracker",
  "version": "2.0.0",
  "description": "KPI Tracker for Support Team",
  "main": "server.js",
  "engines": { "node": ">=18" },
  "scripts": {
    "start": "node --env-file=.env server.js",
    "db:generate": "prisma generate",
    "db:migrate": "prisma migrate dev",
    "postinstall": "prisma generate"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "prisma": "^5.14.0"
  }
}
